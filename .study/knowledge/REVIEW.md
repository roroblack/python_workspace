# Review Queue

복습은 다시 읽기가 아니라, 질문에 직접 답하는 방식으로 한다.

## Python

- Python 인터프리터는 코드를 어떤 단위로 실행하는가?
- `.pyc` 파일은 왜 생기며, 소스 코드와 어떤 관계가 있는가?
- dict는 왜 평균적으로 빠른 조회가 가능한가?
- 얕은 복사와 깊은 복사는 언제 문제가 되는가?
- ABC와 Protocol은 모두 추상화를 다루지만, 어떤 기준으로 다르게 쓰는가?

## MySQL

- DBMS에서 계정, 스키마, 테이블은 왜 분리되어 있는가?
- PRIMARY KEY와 UNIQUE는 비슷해 보이지만 무엇이 다른가?
- SELECT문의 논리적 실행 순서는 어떻게 되는가?
- JOIN을 잘못 쓰면 왜 행 수가 폭발하는가?
- MySQL InnoDB가 B+Tree를 인덱스 구조로 쓰는 이유는 무엇인가?
- 클러스터드 인덱스는 "테이블 자체가 인덱스"라는 말을 어떻게 현실화하는가?

## Crawling

- 정적 크롤링과 동적 크롤링의 기준은 무엇인가?
- HTML 파싱에서 선택자 설계가 깨지는 대표 원인은 무엇인가?
- Selenium/Playwright가 필요한 상황은 어떤 상황인가?
- 크롤링 결과를 DB에 넣을 때 중복과 재실행 문제는 어떻게 막을 수 있는가?

## Data Analysis

- numpy array는 dtype을 어떻게 결정하는가?
- 문자열 dtype 길이를 직접 지정하면 왜 한글이 잘릴 수 있는가?
- NaN은 왜 `==`로 비교하면 안 되는가?
- 결측값을 평균으로 채우면 분산은 어떻게 변하는가?
- 이상값을 제거하는 것과 변환하는 것은 어떤 기준으로 고르는가?
- 정규화와 표준화는 무엇이 다르고 언제 쓰는가?

## Machine Learning

- train/test split은 왜 필요한가?
- 선형회귀를 분류 문제에 그대로 쓰면 어떤 문제가 생기는가?
- 로지스틱 회귀의 sigmoid는 어떤 역할을 하는가?
- SVM에서 margin은 무엇을 의미하는가?
- RBF kernel은 어떤 문제를 해결하는가?
- C와 gamma를 동시에 키우면 어떤 위험이 생기는가?
- 결정트리는 왜 과적합되기 쉬운가?
- Random Forest는 단일 결정트리의 어떤 약점을 줄이는가?
- Boosting은 Bagging과 무엇이 다른가?
- ANN에서 scaling이 중요한 이유는 무엇인가?

## Dimension / Unsupervised

- 차원의 저주는 거리 기반 모델에 어떤 영향을 주는가?
- PCA는 어떤 축을 먼저 선택하는가?
- PCA 전에 스케일링을 해야 하는 이유는 무엇인가?
- 설명분산비는 어떻게 해석해야 하는가?
- t-SNE는 PCA와 달리 어떤 목적에 더 가깝나?
- k-means에서 k를 고르는 방법에는 무엇이 있는가?
- 실루엣 계수는 무엇을 평가하는가?
- support, confidence, lift는 각각 무엇을 말하는가?
- lift가 1보다 크다는 것은 어떤 의미인가?

## Project

- 수소차 프로젝트에서 데이터 수집, 저장, 표시 흐름은 어떻게 이어졌는가?
- 프로젝트에서 ERD가 먼저 필요한 이유는 무엇인가?
- Streamlit 앱에서 상태와 인증을 다룰 때 주의할 점은 무엇인가?
- 실습 코드에서 모델 성능이 좋아졌을 때, 진짜 개선인지 데이터 누수인지 어떻게 확인하는가?

